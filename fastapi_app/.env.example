# ==========================================
# Paper2Any Backend Configuration
# ==========================================
# Note: Frontend configuration is in frontend-workflow/.env
# This file only contains backend-specific environment variables

# 大模型API配置
DF_API_URL=your-api-url-here
DF_API_KEY=your-api-key-here

# ==========================================
# 计费配置（Backend Billing Configuration）
# ==========================================
# 计费开关（默认开启，设为 false 可禁用计费）
BILLING_ENABLED=true

# 全局 SKU ID（标识 App，必填）
# 请联系管理员获取你的 App SKU ID
BILLING_SKU_ID=your-sku-id-here

# 扣费 API 地址
BILLING_API_URL=https://openapi.dp.tech/openapi/v1/api/integral/consume

# 价格配置文件路径（相对于项目根目录）
# 所有价格配置统一在此文件中管理，前后端共享
PRICING_CONFIG_PATH=config/pricing.json

# ==========================================
# 开发者凭证（可选，用于本地调试）
# ==========================================
# 如果不想每次都通过前端登录，可以设置开发者默认凭证
# 注意：这些凭证仅用于本地开发调试，生产环境请通过 Cookie 传递用户凭证
# 
# 使用方法：
# 1. 在命令行中设置环境变量：
#    export DEV_ACCESS_KEY="your_access_key_here"
#    export DEV_CLIENT_NAME="your_client_name_here"
# 
# 2. 或者在 .env 文件中添加（不推荐提交到 Git）：
#    DEV_ACCESS_KEY=your_access_key_here
#    DEV_CLIENT_NAME=your_client_name_here
#
# 凭证优先级：
# 1. Cookie 中的用户凭证（生产环境）
# 2. 环境变量中的开发者凭证（本地调试）
# 3. 未配置则返回 401 错误
#
# DEV_ACCESS_KEY=
# DEV_CLIENT_NAME=
