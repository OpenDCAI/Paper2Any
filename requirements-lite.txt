# Lightweight dependencies for development and testing
# Does NOT include heavy ML models (MinerU, SAM, YOLO, PaddleOCR, etc.)
# Use this for: API development, auth testing, frontend integration

# Core Python utilities
numpy<2.0.0
scipy
tqdm
pydantic
pyyaml
requests
httpx
colorlog
rich
colorama

# FastAPI backend
fastapi
uvicorn
python-multipart

# LangGraph/LangChain (API wrappers only, no local models)
langgraph==0.6.7
langgraph-checkpoint==2.1.1
langgraph-prebuilt==0.6.4
langgraph-sdk==0.2.6
langchain==0.3.27
langchain-community==0.3.29
langchain-core==0.3.76
langchain-openai==0.3.33
langchain-text-splitters==0.3.11

# Supabase integration (auth, rate limiting, storage)
supabase>=2.0.0
python-jose[cryptography]>=3.3.0
pydantic-settings>=2.0.0

# Testing
pytest
pytest-asyncio
httpx[socks]

# Basic image processing (no ML)
pillow
opencv-python-headless  # Headless version, smaller

# Document processing (lightweight)
python-pptx
PyPDF2
beautifulsoup4

# Data processing
pandas

# Gradio UI (optional, comment out if not needed)
# gradio
