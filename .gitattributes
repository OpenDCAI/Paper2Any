# 默认行为：Git 会自动检测文本文件，检出时转换为系统原生格式，提交时转换为 LF
* text=auto

# 强制指定某些文件在提交时必须也是 LF（推荐现代项目都强制使用 LF）
# 这样 Windows 用户检出时可能是 CRLF，但提交回仓库时永远是 LF
* text eol=lf

# 对于二进制文件（图片、PDF等），绝对不要转换换行符
*.png binary
*.jpg binary